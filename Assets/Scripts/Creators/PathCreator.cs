using System.Collections;

using UnityEngine;

public class PathCreator : GeoCreator
{
    public float rounds = 1;
    Vector3 lastPos;
    
    protected override void Draw(Vector3 p1, Vector3 p2, float maxTime, Color col)
    {
        var newPos = 
    }
}
